scalar Date
type Query {
  Post(id: ID!): Post!
  Posts: [Post]
}

type Post {
  id: ID!
  content: String!
  CreatedAt: Date!
  User: User!
}

type Mutation {
  CreatePost(data:CreatePostInput): Post!
}

input CreatePostInput {
  content: String
  userId: ID!
}
