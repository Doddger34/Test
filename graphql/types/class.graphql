scalar Date
type Query {
    Class(id: ID!): Class
    Classes: [Class!]!
}

type Class {
    id: ID!
    ClassName: Int!
    createdAt: Date
    Not: [Not]
    Post: [Post]
}

type Mutation {
    CreteClass(data: CreateClassInput): Class!
}

input CreateClassInput {
    ClassName: Int!
} 
